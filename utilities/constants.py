# todo: fill this info out. I don't have to mention that
#   THOU SHALL NOT COMMIT DATA OF SENSITIVE NATURE FOR OBVIOUS REASONS
import os
from dotenv import load_dotenv
load_dotenv()

user_name = os.environ.get('USER')
password = os.environ.get('PASSWORD')
